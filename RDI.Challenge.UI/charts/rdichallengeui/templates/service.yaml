apiVersion: v1
kind: Service
metadata:
  name: {{ template "rdichallengeui.fullname" . }}
  labels:
    app: {{ template "rdichallengeui.name" . }}
    chart: {{ template "rdichallengeui.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "rdichallengeui.name" . }}
    release: {{ .Release.Name }}
